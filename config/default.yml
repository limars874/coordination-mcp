# Coordination MCP 默认配置。
# 可通过 ~/.coordination-mcp/config.yml 或 --profile 指定另一个 YAML 文件覆盖这里的配置。
# 未配置 dataDirectory 时默认使用 ~/.coordination-mcp/data。
port: 3000
allowedHosts:
  - 127.0.0.1
  - localhost
